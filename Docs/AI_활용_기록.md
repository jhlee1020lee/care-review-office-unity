# AI 활용 기록

작성일: 2026-06-05

이 문서는 프로젝트 작업 중 AI를 활용한 내용을 계속 누적 기록하는 문서다.

## 개요

`돌봄지원 심사소` Unity 프로젝트 개발과 백업 과정에서 ChatGPT/Codex를 보조 개발 도구로 활용했다. 사용자는 구현 방향, 저장소 생성, 백업 진행 여부를 결정했고, AI는 로컬 프로젝트 점검, 코드 수정, 자산 생성, 테스트 실행, 문서화, Git 백업 작업을 수행했다.

## 대화 및 작업 요약

### 2026-06-05

- 사용자는 게임에 건조한 사무실 톤의 배경음악과 효과음을 추가하고 싶다고 요청했다.
- AI는 외부 저작권 음원을 사용하지 않고, 로컬 합성 스크립트로 WAV 오디오 자산을 생성하는 방향을 제안하고 구현했다.
- AI는 Unity 프로젝트의 기존 `CareReviewGame.cs` 구조를 확인한 뒤, WAV 자산을 우선 로드하고 없을 때만 절차적 오디오를 폴백으로 쓰도록 수정했다.
- AI는 BGM 5종, SFX 20종을 생성하고 화면별 BGM 전환 및 UI/도장/사건/저장/오류 효과음을 연결했다.
- AI는 오디오 스모크 테스트를 확장해 자산 로드 여부, 샘플 수, 화면별 BGM 키, 음량 0 처리 등을 검증했다.
- 사용자는 작업 내용을 백업하기 위해 GitHub 저장소를 만들었고, AI는 로컬 git 초기화, `.gitignore` 작성, 커밋, 원격 저장소 푸시를 수행했다.
- 사용자의 요청에 따라 AI는 프로젝트 README를 작성하고 GitHub에 커밋 및 푸시했다.
- 사용자는 앞으로도 AI 활용 내역을 계속 기록해야 한다고 요청했고, AI는 이 문서를 누적 기록 형식으로 정리했다.

### 2026-06-07

- 사용자는 게임 내 한국어 표시 문구가 부자연스럽다고 판단하고, Claude Code를 활용해 전체 UI 문구를 자연스럽게 다듬는 방향을 검토했다.
- Claude Code는 `Assets/Scripts/CareReviewGame.cs`의 사용자 표시 문구를 중심으로 영어 혼용 표현과 용어 불일치를 정리했다.
- 표시 문구 변경에 맞춰 자체 점검용 `.Contains(...)` 리터럴도 함께 수정해 테스트 조건이 기존 의미를 유지하도록 했다.
- AI는 `Docs/한국어_문구_정리_내역.md` 문서를 작성해 변경 원칙, 주요 수정 항목, 유지한 용어, 확인 필요 항목을 기록했다.
- Codex는 해당 문서와 실제 git diff를 검토해 로직 변경 없이 문자열 중심 변경으로 제한된 것을 확인했다.
- 사용자는 일부 굵은 글자가 지나치게 두꺼워 읽기 어렵다고 지적했고, Codex는 Paperlogy 세미볼드 폰트에 `FontStyle.Bold`가 중복 적용되는 구조를 확인했다.
- Codex는 텍스트 생성 헬퍼에서 Paperlogy 사용 시 합성 Bold를 제거하도록 조정해 제목, 버튼, 본문 전반의 획 뭉침을 완화했다.
- 사용자는 일부 텍스트가 박스 경계 밖으로 조금씩 삐져나와 보인다고 지적했고, Codex는 텍스트 박스 내부 패딩과 버튼 라벨 안전 영역을 늘려 경계 여백을 확보했다.
- 이후 사용자는 문제가 심사 화면 서류 카드의 `신청서`/`소득증명` 라벨 영역이라고 구체화했고, Codex는 해당 라벨을 카드와 분리된 작은 배경 띠 안에 배치하도록 위치, 크기, 글자 크기를 조정했다.
- 사용자는 실제 출시를 위해 플레이어에게 불필요한 기능을 제거하고 게임 플레이에 필요한 항목만 남기자고 요청했다.
- Codex는 1차 출시 UI 정리로 설정, 심사 기록, 후속 연락함, 최종 리포트에서 `로그 저장`, `로그 폴더`, `환경 진단`, `지원 번들`, `FPS`, 플레이테스트 설문 진입 경로를 일반 플레이어 화면에서 제거했다.
- QA/스모크 테스트에 필요한 플레이테스트 설문 UI는 일반 실행에서는 만들지 않고 `-careReview...` 명령줄 실행 때만 생성되도록 분리했다.
- 사용자는 심사 화면 중앙 신청서 하단의 `신청서`/`소득증명` 서류 상태 박스가 여전히 어색하다고 지적했고, Codex는 해당 박스의 높이, 패딩, 글자 크기를 조정해 서류 목록이 경계에 걸리지 않도록 수정했다.
- 이후에도 서류 라벨이 경계에 붙어 보이는 문제가 남아 있어, Codex는 상단 서류 카드 라벨에 마스크와 자동 축소를 적용하고 중앙 서류 상태 박스의 인셋을 넓혀 `신청서`/`소득증명` 텍스트가 장식선 밖으로 보이지 않도록 재조정했다.
- 출시 UI 정리에 맞춰 크레딧, 첫 실행 고지, 접근성 스모크 테스트의 기대값도 개발용 로그/진단/지원 번들 문구가 일반 플레이어 화면에 노출되지 않는 기준으로 수정했다.
- Codex는 최종 리포트와 캠페인 기록에 남아 있던 `에이전트 분석`, `결정 감사`, `대시보드`, `CSV` 중심 표현을 플레이어용 `판단 비교`, `판단 복기`, `심사 기록` 표현으로 다듬었다.
- Codex는 성과와 캠페인 기록 화면의 `Steam 후보`, `상점 후보`, `후보 전환` 같은 출시 준비용 표현을 `성과 진행`, `기록 요약`, `추천 전환` 등 플레이어용 표현으로 바꿨다.
- Codex는 보정 목표와 성과 기록 화면에 남아 있던 `후보`, `보정 분류` 표현을 플레이어가 이해하기 쉬운 `추천`, `보정 추천` 표현으로 정리하고, 변경된 UI에 맞춰 성과/포커스 스모크 테스트 기대값을 수정했다.
- Codex는 README의 게임 소개를 현재 인게임 용어인 `심사 기록`, `판단 비교`, `판단 복기`에 맞게 고치고, 성과 기록의 `판단 복기/판단 비교 확인` 목표가 개발용 로그 저장이 아니라 실제 화면을 열도록 연결과 스모크 테스트를 정리했다.
- Codex는 출시 후보 캡처와 상점 제출 문서에서 QA 전용 `09_playtest_survey.png` 설문 화면을 제거하고, 상점 후보 스크린샷 기준을 실제 플레이어 화면 중심의 5장 세트로 정리했다.
- Codex는 패키지 README 생성 문구와 Steam 상점 자료/제출안의 기능 설명에서 로그 저장, HTML 대시보드, 환경 진단, 지원 번들 같은 개발·QA용 표현을 줄이고 플레이어가 실제로 보는 리포트, 성과 기록, 캠페인 기록, 판단 비교/복기 중심으로 다시 정리했다.
- 사용자는 심사 화면의 `신청서`/`소득증명` 영역이 여전히 어색하다고 지적했고, Codex는 상단 서류 카드의 첫 두 슬롯을 항상 `신청서`, `소득증명`으로 고정한 뒤 보완/위험/근무표 같은 상태 라벨을 뒤쪽 슬롯으로 옮겼다. 중앙 서류 상태 박스도 더 넓은 종이 영역 안에 배치하고, Windows 빌드와 저해상도 UI 스모크 테스트로 39장 캡처 기준을 확인했다.
- Codex는 실제 출시용 패키지 README가 QA 명령어 목록이 아니라 플레이어 실행 안내, 기본 조작, 데이터 안내 중심으로 생성되도록 바꿨다. Steam 상점 자료와 제출안에서도 `HTML 대시보드`, `에이전트 분석`, `결정 감사 대시보드`, `상점 후보 스크린샷`처럼 개발/QA 목적이 섞인 표현을 플레이어용 `판단 비교`, `판단 복기`, `QA 비교 스크린샷` 중심으로 정리했다.
- Codex는 README 생성기에 남아 있던 죽은 QA 안내 블록을 제거하고, Steam 업적 후보 생성기의 `로그 분석 패킷 생성`, `가상 심사 실험실` 항목을 실제 게임 내 성과와 같은 `판단 복기 확인`, `판단 비교 확인`으로 맞췄다. Windows 빌드, 릴리즈 패키징, Steamworks depot 초안 생성을 실행해 패키지 README와 업적 후보 CSV/Markdown이 플레이어용 문구로 생성되는지 확인했다.
- Codex는 릴리즈 패키지의 `RELEASE_MANIFEST.txt`에 남아 있던 플레이테스트, 지원 번들, 환경 진단, 스모크 테스트 등 내부 QA 목록을 제거하고, 실행 파일/런타임 폴더/폰트 라이선스/플레이어 기능/로컬 데이터 안내 중심의 매니페스트로 재생성했다. `PackageCurrentWindowsBuild`와 `PrepareSteamworksDepot`를 실행한 뒤 릴리즈 패키지와 Steamworks content 사본 모두에서 내부용 금지 문구가 검색되지 않는지 확인했다.
- Codex는 배포 무결성 감사에서 플레이어 릴리즈 zip과 무관한 플레이테스트 키트 사본 동기화 항목을 필수 조건에서 제거하고, 상점 자료 사본 감사 조건을 `보정 추천`, `QA 비교` 등 플레이어/검수용으로 정리된 현재 문구에 맞췄다. `AuditDistributionIntegrity`는 118/118 통과했고, `AuditReleaseCandidate`는 로컬 릴리즈 후보 `localReady=True`로 확인됐다.
- 2026-06-07: 사용자가 심사 화면의 `신청서`/`소득증명` 라벨이 여전히 박스 안에서 어색하다고 지적했고, Codex는 상단 서류 카드 라벨을 더 작은 고정 라벨 칩으로 바꾸고 서류 상태 박스의 안쪽 여백을 조정했다. Windows 빌드와 그래픽 모드 저해상도 UI 스모크 테스트를 실행해 1280x720 캡처에서 라벨이 칩 안에 들어오는지 확인했다.
- 2026-06-07: Codex는 `ExportLog()`가 호출될 때 최종 리포트 화면에 `로그 저장`, `테스터 설문`, `HTML 대시보드` 같은 내부 회수 문구를 덧붙일 수 있는 경로를 제거했다. 자동 테스트용 파일 생성은 유지하되 플레이어 리포트 안내는 그대로 두도록 바꾸고, 접근성 스모크에 `ExportLog()` 호출 후에도 리포트 안내가 플레이어용 문구로 유지되는 검사를 추가했다.
- 2026-06-07: Codex는 최종 리포트의 `판단 비교` 화면과 캠페인 기록 상세에서 `자동 플레이`, `플레이어 성향`, `성향 목표`처럼 개발 모델이 드러나는 표현을 `내 판단`, `가까운 기준`, `다음 회차 추천` 중심의 플레이어용 문구로 바꿨다. 관련 판단 비교/다음 회차 추천/캠페인 기록 스모크 테스트를 다시 실행해 문구 변경 후에도 화면 연결과 추천 적용이 동작하는지 확인했다.
- 2026-06-07: 사용자가 서류 카드의 `신청서`/`소득증명` 라벨과 성과·기록 화면의 축약 문구가 여전히 어색하다고 지적했다. Codex는 상단 서류 카드 라벨을 더 넓은 칩 안에 한 줄로 고정하고 `소득증명`을 온전한 라벨로 유지했으며, `복기 W-207`, `첫 안내`처럼 개발 과정의 축약 표기가 보이던 경로를 `복기 W-207 기록 복귀` 같은 플레이어용 문구로 바꿨다. Windows 빌드, 캠페인 기록 스모크, 저해상도 UI 스모크, 포커스 이동 스모크를 실행하고 그래픽 모드 1280x720 캡처로 서류 라벨 배치를 확인했다.
- 2026-06-07: Codex는 출시용 UI 점검을 이어서 진행하며 포커스 이동 스모크 결과에 내부 버튼 오브젝트명(`Tutorial Back`)이 남는 문제를 표시 라벨 기준 기록으로 바꿨다. 또한 기준표 화면의 `정책 시뮬레이터`, `재시뮬레이션` 표현을 플레이어용 `기준 비교`, `40건 전체 기준 비교`로 바꾸고, 정책 비교 스모크 결과가 `Builds/QA` 미러에도 최신 문구로 저장되도록 보강했다.
- 2026-06-08: Codex는 일반 플레이어 화면에 남아 있던 `로그에 남겼습니다`, `분석 코멘트`, `분석 그룹`, `세부 분석 기록 없음` 표현을 `심사 기록에 남겼습니다`, `결과 해석`, `판단 그룹`, `세부 기록 없음`으로 정리했다. 상점 자료와 한국어 문구 정리 문서도 현재 인게임 용어에 맞춰 갱신했다.
- 2026-06-08: 사용자가 심사 화면 중앙 서류 상태 박스의 `신청서`/`소득증명` 줄이 여전히 어색하다고 지적했다. Codex는 신청서 본문과 서류 상태 박스의 높이, 위치, 안쪽 여백, 글자 크기를 다시 조정해 서류 목록이 장식선과 겹치지 않게 했고, Windows 빌드와 그래픽 모드 저해상도 UI 스모크 테스트를 실행해 1280x720 심사 화면 캡처를 확인했다.
- 2026-06-08: Codex는 출시용 화면 점검 중 메인 메뉴와 크레딧에 표시되는 버전명이 `v0.3.0 내부 콘텐츠 빌드`로 남아 있는 것을 확인하고 `v0.3.0`으로 정리했다. 상점 자료와 제출안의 대상 버전 표기도 같은 방식으로 맞췄으며, Windows 빌드, 기본 스모크, UI 정리 스모크, 크레딧 스모크, 그래픽 모드 저해상도 UI 스모크로 확인했다.
- 2026-06-08: 사용자가 심사 화면 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 이상하다고 지적했고, Codex는 `소득증명`을 온전한 라벨로 유지하면서 라벨 칩 폭, 높이, 글자 크기, 자동 축소 범위를 다시 조정했다. Windows 빌드와 그래픽 모드 저해상도 UI 스모크 테스트를 실행해 1280x720 캡처에서 두 라벨이 칩 안에 들어오는지 확인했다.
- 2026-06-08: Codex는 출시용 문구 정리의 후속으로 플레이어 화면과 생성 문서에 남아 있던 `결정 감사 코칭`, `코칭 목표`, `다음 회차 코칭` 계열 표현을 `판단 복기`, `복기 목표`, `다음 회차 복기` 중심으로 정리했다. 오래된 플레이테스트 세션에서 읽어 온 문구는 집계 단계에서 `NormalizeReviewTerms`로 정규화하도록 보강했다.
- 2026-06-08: Codex는 추천 회차 복기와 판단 복기 HUD 스모크 결과가 `Builds/QA` 사본에 최신 JSON으로 저장되도록 보강하고, 첫 브리핑에 판단 복기 목표와 압박 패턴이 표시되는지 검증했다. 이후 플레이테스트 패킷, 집계, Steamworks 준비, 배포 무결성 감사, 릴리즈 후보 감사를 다시 실행해 로컬 릴리즈 후보가 258/258 통과하고 배포 무결성이 118/118 통과하는 것을 확인했다.
- 2026-06-08: Codex는 설정 화면에 일반 플레이어가 쓸 필요 없는 `로그 폴더`, `환경 진단`, `지원 번들`, `로그 저장` 버튼이 실제 활성 버튼 라벨로 남아 있지 않은지 세는 검증을 추가했다. 접근성 스모크 결과를 `Builds/QA`에 미러하고 릴리즈 후보 감사에 `settingsHiddenToolButtonCount=0` 조건을 연결한 뒤, Windows 빌드, 접근성 스모크, 릴리즈 패키징, Steamworks 준비, 릴리즈 후보 감사, 배포 무결성 감사를 다시 실행했다.
- 2026-06-08: Codex는 `-careReview...` QA/스모크 실행 인자가 일반 플레이어 실행에서도 인식될 수 있는 구조를 확인하고, 해당 인자 처리를 배치모드 실행으로 제한했다. 배치모드 테스트는 유지하되 일반 실행에서는 같은 인자를 무시하도록 헬퍼를 분리하고, 접근성 스모크에 `nonBatchCareReviewArgsIgnored=true` 검증을 추가했다. 이후 Windows 빌드, 접근성 스모크, 릴리즈 패키징, Steamworks 준비, 배포 무결성 감사, 릴리즈 후보 감사를 다시 실행해 로컬 릴리즈 후보 259/259와 배포 무결성 118/118을 확인했다.
- 2026-06-08: Codex는 설정, 크레딧/고지, 최종 리포트의 실제 활성 UI 텍스트에 내부/QA 문구가 보이는지 세는 검증을 추가했다. 이 과정에서 최종 리포트 재검토 큐의 `테스터에게는 이 사례가 어렵거나 납득됐는지 먼저 묻습니다.` 문구가 잡혀, 플레이어용 `어렵거나 납득하기 애매했던 사례부터 다시 확인하세요.`로 교체했다. 접근성 스모크의 `visibleReleaseHiddenTextCount=0`, 릴리즈 후보 감사 259/259, 배포 무결성 감사 118/118을 확인했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 이상하다고 재지적했고, Codex는 라벨 칩에 `RectMask2D`를 추가하고 긴 라벨을 한 줄 자동 축소로 고정해 카드 밖으로 삐져나올 수 있는 경로를 막았다. 이후 Windows 빌드, 접근성 스모크, 릴리즈 패키징, Steamworks 준비, 배포 무결성 감사, 릴리즈 후보 감사를 다시 실행해 `visibleReleaseHiddenTextCount=0`, 로컬 릴리즈 후보 259/259, 배포 무결성 118/118을 확인했다.
- 2026-06-08: Codex는 출시 정리 목표에 맞춰 일반 플레이어 실행에서 `ExportLog()`가 플레이테스트 피드백 Markdown, 요약 JSON, HTML 분석 대시보드 같은 QA 산출물을 생성할 수 있는 구조를 확인했다. 기본 심사 로그 저장은 유지하되 QA export 산출물은 `-careReview...` 배치 자동화 실행에서만 생성되도록 분기하고, 접근성 스모크와 릴리즈 후보 감사에 `nonBatchQaExportArtifactsSkipped=true` 조건을 추가했다. Windows 빌드, 기본 스모크, 접근성 스모크, 릴리즈 패키징, Steamworks 준비, 배포 무결성 감사 118/118, 릴리즈 후보 감사 259/259을 다시 확인했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 박스 안에 자연스럽게 들어가지 않는다고 재지적했다. Codex는 라벨 칩의 안쪽 텍스트 영역을 더 줄이고 `Overflow`를 제거했으며, `소득증명`은 좁은 칩 안에서 `소득/증명` 2줄로 표시되도록 바꿨다. Windows 빌드와 저해상도 UI 스모크 테스트를 실행해 컴파일 성공, `buttonTextOverflowCount=0`, `offscreenButtonCount=0`, `invalidScreenshotCount=0`을 확인했다.
- 2026-06-08: Codex는 릴리즈 패키지와 Steamworks content에 `.pdb`, `.mdb`, `.log`, `.tmp`, QA, smoke, playtest, support bundle, system diagnostic 산출물이 섞이지 않는지 확인하는 릴리즈 후보 감사 조건을 추가했다. 최신 Windows 빌드를 다시 패키징하고 Steamworks content를 재생성한 뒤 배포 무결성 감사 118/118, 릴리즈 후보 감사 260/260을 확인했으며, 실제 패키지 파일명과 플레이어용 텍스트 파일에서도 내부 QA 문구가 검색되지 않는지 다시 검증했다.
- 2026-06-08: Codex는 릴리즈 플레이어 빌드 안에 QA 자동화 실행 경로가 남는지 다시 점검했다. `CARE_REVIEW_QA_TOOLS` 정의가 있을 때만 `-careReview...` 자동화 인자와 QA export 경로를 참조하도록 분리하고, 릴리즈 빌드는 빌드 폴더를 비운 뒤 생성하며 `.pdb/.mdb` 디버그 심볼을 제거하도록 보강했다. IL2CPP 모듈은 현재 Unity 설치에 없어 Mono 릴리즈로 유지했지만, 릴리즈/Steamworks 어셈블리에 `-careReview`, `playtest_packet`, `support_bundle`, `system_diagnostic` 문자열이 남지 않는 감사 조건을 추가했고 릴리즈 후보 감사 261/261, 배포 무결성 감사 118/118을 확인했다.
- 2026-06-08: Codex는 일반 릴리즈 실행 중 Warning/Error가 발생할 때 `care_review_runtime_issues.*` 파일이 자동 생성될 수 있는 경로를 확인했다. 해당 런타임 이슈 로그 구독은 QA 도구 빌드에서만 활성화되도록 바꾸고, 별도 QA 도구 빌드에서 접근성 스모크가 계속 동작하는지 확인했다. 이후 릴리즈 빌드, 패키징, Steamworks content 재생성, 배포 무결성 감사 118/118, 릴리즈 후보 감사 261/261을 다시 실행했으며 릴리즈/Steamworks 어셈블리에 `care_review_runtime_issues`, `CARE_REVIEW_SUPPORT_BUNDLE`, `CARE_REVIEW_SYSTEM_DIAGNOSTIC` 문자열이 남지 않는지 확인했다.
- 2026-06-08: Codex는 플레이테스트 설문 UI가 일반 릴리즈에서 생성되거나 입력/포커스 경로에 남는지 점검했다. 리포트 화면의 설문 단축키, 컨트롤러 처리, 포커스 루트, 로그 저장 후 설문 상태 갱신을 QA 도구 빌드에서만 컴파일되게 정리했고, QA 도구 빌드에서는 플레이테스트 설문 스모크가 계속 통과하는지 확인했다. 이후 릴리즈 빌드, 패키징, Steamworks content 재생성, 배포 무결성 감사 118/118, 릴리즈 후보 감사 261/261을 다시 실행했으며 릴리즈/Steamworks 어셈블리에 `플레이테스트 설문`, `설문 저장`, `익명 테스터`, `테스터 코드` 문자열이 남지 않는지 확인했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 어색하다고 재지적했다. Codex는 작은 라벨 박스 구조를 제거하고, 각 서류 슬롯 하단에 직접 캡션을 배치하도록 바꿨으며 `소득증명`도 한 줄 라벨로 되돌렸다. QA 도구 Windows 빌드와 저해상도 UI 스모크는 통과했지만, 현재 실행 환경에서는 자동 스크린샷 PNG가 검은 화면으로 저장되어 실제 시각 확인은 수동 플레이 화면에서 다시 봐야 한다고 기록했다.
- 2026-06-08: Codex는 저해상도 UI 스모크가 검은 PNG도 파일 크기만으로 유효 캡처로 처리할 수 있는 문제를 확인했다. QA 도구 빌드에서만 저해상도/상점 캡처 같은 시각 검증 인자를 일반 그래픽 실행에서도 허용하고, 스크린샷 검증은 픽셀 밝기와 변화량까지 검사하도록 보강했다. README의 스모크 테스트 안내도 릴리즈 `Builds/Windows`가 아니라 QA 도구 빌드 `Builds/Windows_QA`를 쓰도록 정정했다. 이후 QA 도구 빌드, 일반 그래픽 저해상도 UI 스모크, 릴리즈 빌드, 패키징, Steamworks content 재생성, 배포 무결성 감사 118/118, 릴리즈 후보 감사 261/261을 다시 확인했으며 릴리즈/Steamworks 어셈블리에 `-careReview`, `support_bundle`, `system_diagnostic`, `care_review_runtime_issues`, `지원 번들`, `환경 진단`, `로그 폴더` 문자열이 남지 않는지 확인했다.
- 2026-06-08: 사용자가 `신청서`/`소득증명` 영역이 여전히 어색하다고 다시 지적했다. Codex는 상단 서류 슬롯 캡션의 높이와 글자 크기를 키우고, 중앙 서류 상태 박스의 높이, 패딩, 줄 간격을 늘려 서류 목록이 장식선에 눌려 보이지 않도록 조정했다. QA 도구 Windows 빌드와 일반 그래픽 저해상도 UI 스모크를 다시 실행해 `invalidScreenshotCount=0`, `buttonTextOverflowCount=0`, `offscreenButtonCount=0`을 확인하고 1280x720 심사 화면 PNG를 직접 열어 확인했다.
- 2026-06-08: Codex는 출시 패키지와 Steamworks content의 `README_KR.txt`, `RELEASE_MANIFEST.txt`에 내부/QA 문구가 다시 섞여도 기존 감사가 직접 잡지 못하는 구멍을 확인했다. 릴리즈 후보 감사와 배포 무결성 감사에 플레이어 안내 텍스트 금지어 검사를 추가해 `-careReview`, `care_review`, `playtest`, `support_bundle`, `system_diagnostic`, `스모크`, `지원 번들`, `환경 진단`, `로그 저장`, `CSV/JSON/HTML 대시보드` 계열 문구가 포함되면 실패하도록 했다. 릴리즈 빌드, 패키징, Steamworks content 재생성, 배포 무결성 감사 119/119, 릴리즈 후보 감사 262/262를 확인했고 릴리즈/Steamworks 어셈블리와 플레이어 안내 파일을 별도 검색해 내부 QA 문구가 없음을 확인했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 어색하다고 지적했다. Codex는 릴리즈 컴파일을 먼저 복구한 뒤, 각 서류 슬롯의 아이콘과 라벨을 분리하고 라벨을 별도 배경판 중앙에 고정해 텍스트가 슬롯 밖으로 밀려 보이지 않도록 수정했다. QA 도구 Windows 빌드, 일반 그래픽 저해상도 UI 스모크, 릴리즈 Windows 빌드를 실행했고 `invalidScreenshotCount=0`, `buttonTextOverflowCount=0`, `offscreenButtonCount=0` 및 1280x720/1920x1080 심사 화면 PNG를 직접 확인했다.
- 2026-06-08: 사용자가 같은 서류 라벨이 여전히 이상하다고 다시 지적했다. Codex는 릴리즈 빌드의 QA/플레이테스트 자동화 코드 분리 중 깨진 전처리 블록을 복구하고, 상단 서류 슬롯을 더 작은 독립 카드처럼 보이도록 간격, 라벨판 폭, 글자 크기, 자동 축소 방식을 다시 조정했다. 릴리즈 Windows 빌드, QA 도구 Windows 빌드, 일반 그래픽 저해상도 UI 스모크를 실행해 `invalidScreenshotCount=0`, `buttonTextOverflowCount=0`, `offscreenButtonCount=0`을 확인하고 1280x720 심사 화면 PNG를 직접 열어 `신청서`/`소득증명` 라벨 배치를 확인했다.
- 2026-06-08: Codex는 최신 Windows 릴리즈 빌드와 Steamworks content 사본을 다시 대조해 이전 Steamworks 사본에 남아 있던 `playtest`, `survey`, `CSV`, `HTML`, `QA` 문자열을 확인했다. 최신 릴리즈를 패키징하고 Steamworks content를 재생성한 뒤, 게임 어셈블리 `Assembly-CSharp.dll` 세 곳에서 자동화/export/playtest/survey/CSV/HTML/QA 문자열이 남지 않는 감사를 추가했다. 릴리즈 후보 감사는 `localReady=True`, `localBlockerCount=0`, 배포 무결성 감사는 119/119 통과로 확인했다.
- 2026-06-08: Codex는 릴리즈 후보 감사에서 실제 Steam 제출 전 필수 외부 항목과 선택적 사람 플레이테스트 검증 항목이 한 목록에 섞여 있어 출시 판단이 불분명한 문제를 정리했다. `externalActionCount`는 Steam AppID/DepotID, SteamCMD, 비공개 브랜치, Steam 클라이언트 QA, Store Presence, Achievements, 외부 저사양 PC 검증 8개만 세도록 하고, 5명/10명 사람 플레이테스트 회수는 `optionalExternalValidationCount=2`로 별도 기록하게 했다. Steamworks 프리플라이트와 사람용 문서의 Store Presence 상호참조도 `externalActionCount=8`로 맞췄으며, 릴리즈 후보 감사 262/262 통과와 배포 무결성 감사 119/119 통과를 확인했다.
- 2026-06-08: Codex는 최신 UI/릴리즈 감사 정리 변경이 실제 플레이어용 Windows 빌드와 Steamworks 제출 사본에 반영되지 않은 상태를 확인했다. 플레이어용 `Builds/Windows`를 다시 빌드하고 `Builds/Release/CareReviewOffice_Windows_v0.3.0.zip`, `Builds/Steamworks/v0.3.0`, `Builds/Steamworks/CareReviewOffice_Steamworks_v0.3.0.zip`을 모두 최신 릴리즈 빌드 기준으로 재생성했다. Steamworks 폴더 재생성 시 zip/SHA도 함께 갱신되도록 빌더를 보강했고, 릴리즈 어셈블리 세 곳에서 `-careReview`, `playtest_packet`, `support_bundle`, `system_diagnostic`, `care_review_runtime_issues`, `ExportLog`, `survey`, `CSV`, `HTML`, `QA` 문자열이 byte-level 검사로 검출되지 않음을 확인했다. 최종 릴리즈 후보 감사는 262/262, 배포 무결성 감사는 119/119 통과했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 이상하다고 지적했다. Codex는 `소득증명`을 `소득 증명`으로 띄어 쓰고, 라벨 배경판과 텍스트 영역을 넓히면서 자동 축소와 래핑 기준을 조정했다. 저해상도 UI 스모크에 `evidenceLabelOverflowCount` 검사를 추가했고, 릴리즈 Windows 빌드, QA 도구 Windows 빌드, 일반 그래픽 저해상도 UI 스모크를 실행해 `completed=true`, `evidenceLabelOverflowCount=0` 및 1280x720 심사 화면 캡처를 확인했다.
- 2026-06-08: Codex는 출시 패키지에 플레이어에게 필요 없는 최상위 파일이 섞여도 기존 감사가 파일명 금지어 위주로만 잡는 빈틈을 확인했다. 릴리즈 폴더, 릴리즈 zip, Steamworks `content_windows` 폴더와 zip 내부 `content_windows` 루트가 Unity 런타임, 실행 파일, `README_KR.txt`, `RELEASE_MANIFEST.txt`만 포함하는지 확인하는 allowlist 감사를 추가했다. 배포 무결성 감사는 120/120, 릴리즈 후보 감사는 263/263 통과했고 로컬 릴리즈 후보 상태는 `localReleaseCandidateReady=true`로 유지됐다.
- 2026-06-08: Codex는 Unity `ProjectSettings`와 빌드 산출물 `app.info`에 `SNU Final Project Prototype` 회사명이 남아 있어 신규 플레이어의 LocalLow 저장 경로에 프로토타입 흔적이 노출되는 문제를 확인했다. 빌더와 ProjectSettings의 company/product 메타데이터를 출시명 `Care Review Office`로 맞추고, 릴리즈 빌드/릴리즈 zip/Steamworks content/Steamworks zip의 `app.info`가 같은 이름을 쓰는지 감사 조건을 추가했다. 새 저장 경로 `LocalLow/Care Review Office/Care Review Office`에서 QA 저해상도 UI 스모크를 실행해 `completed=true`를 확인했으며, 신규 플레이어 메인 메뉴 안내가 너무 길어지던 문구도 짧게 정리했다. 최종 배포 무결성 감사는 121/121, 릴리즈 후보 감사는 264/264 통과했다.
- 2026-06-08: Codex는 인게임 고지가 `서버 자동 업로드 없음`이라고 안내하는 반면 Unity `ProjectSettings`의 `submitAnalytics`가 켜져 있는 문제를 확인했다. `submitAnalytics`, Unity Analytics/Ads 초기화, Unity Connect 진단/성능 보고 설정을 출시용 비활성 상태로 정리하고, ProjectSettings와 UnityConnectSettings가 자동 업로드/Analytics 비활성 상태인지 검사하는 감사를 추가했다. 릴리즈 빌드, 릴리즈 패키징, Steamworks content/zip 재생성을 실행했고, 배포 무결성 감사는 122/122, 릴리즈 후보 감사는 265/265 통과했다.
- 2026-06-08: Codex는 `Assets/Resources/Art`에 구버전 fallback 이미지와 생성 소스 시트가 남아 있어 플레이어 빌드에 불필요하게 포함될 수 있는 문제를 확인했다. `family_portraits_sheet.png`, `incident_cards_sheet.png`, `documents_stamps_sheet.png`, `ui_panels_sheet.png`, `ui_chrome_sheet_generated_source.png`를 `Assets/Archive/DeprecatedResourceArt`로 옮기고, 런타임/빌더의 구버전 fallback 참조를 제거했다. Resources에 폐기 리소스가 다시 들어오면 실패하는 감사를 추가했으며, 릴리즈 빌드, 릴리즈 패키징, Steamworks content/zip 재생성, QA 도구 빌드, 저해상도 UI 스모크를 실행해 `completed=true`를 확인했다. 최종 배포 무결성 감사는 123/123, 릴리즈 후보 감사는 266/266 통과했다.
- 2026-06-08: 사용자가 상단 서류 카드의 `신청서`/`소득 증명` 캡션이 여전히 어색하다고 지적했다. Codex는 넓은 라벨 박스 구조를 제거하고 각 서류 아이콘 아래에 직접 캡션을 붙이는 방식으로 바꿨으며, `소득 증명`은 좁은 슬롯에서 `소득/증명` 두 줄로 보이게 조정했다. QA 도구 Windows 빌드와 그래픽 모드 저해상도 UI 스모크를 실행해 `completed=true`, `evidenceLabelOverflowCount=0`, `invalidScreenshotCount=0`을 확인하고 1280x720/1920x1080 심사 화면 캡처를 직접 열어 확인했다.
- 2026-06-08: Codex는 출시 정리 목표에 맞춰 `Assets/Resources`가 현재 플레이어 런타임에 필요한 아트, 오디오, 사례 데이터, 폰트만 담는지 전체 allowlist로 검사하는 감사를 추가했다. 기존 폐기 리소스 5개 제외 검사만으로는 새 불필요 파일이 들어와도 잡기 어려워, 릴리즈 후보 감사와 배포 무결성 감사에 `플레이어 Resources 런타임 자산 allowlist` 항목을 연결했다. 감사 실행 결과 릴리즈 후보는 267/267, 배포 무결성은 124/124 통과했고, 로컬 릴리즈 후보 블로커는 0개로 유지됐다.
- 2026-06-08: Codex는 `Builds/Release`에 남아 있던 구버전 `CareReviewOffice_Windows_v0.2.0` 폴더와 zip이 최신 플레이어 배포물 검사 중 내부 문구로 잡히는 것을 확인했다. 패키징 단계에서 현재 `v0.3.0` 릴리즈 폴더, zip, SHA 파일만 남기고 이전 버전 산출물을 자동 삭제하도록 보강했으며, 릴리즈 후보 감사와 배포 무결성 감사에 `플레이어 릴리즈 이전 버전 산출물 제외` 항목을 추가했다. 최신 패키징과 Steamworks content 재생성 후 `Builds/Release`에는 v0.3.0 산출물만 남았고, 릴리즈 후보는 268/268, 배포 무결성은 125/125 통과했다.
- 2026-06-08: Codex는 Unity 패키지 의존성을 점검해 코드에서 사용하지 않는 `com.unity.multiplayer.center`가 플레이어 빌드의 `Unity.Multiplayer.Center.Common.dll`을 끌고 들어오는 것을 확인했다. `Packages/manifest.json`과 `Packages/packages-lock.json`에서 해당 패키지를 제거하고, 패키지 파일과 플레이어 빌드/릴리즈/Steamworks `ScriptingAssemblies.json` 및 파일명에 `Unity.Multiplayer.Center` 흔적이 남지 않는 감사를 추가했다. Windows 빌드, 릴리즈 패키징, Steamworks content 재생성, 배포 무결성 감사 126/126, 릴리즈 후보 감사 269/269를 실행했고 로컬 블로커는 0개로 유지됐다.
- 2026-06-08: Codex는 `Packages/manifest.json`에 남아 있던 지형, VR/XR, 비디오, 차량, 클로스, 파티클, 웹리퀘스트, Analytics 등 플레이어 코드에서 쓰지 않는 Unity 모듈 의존성을 정리했다. manifest는 `ugui`, `audio`, `imageconversion`, `jsonserialize`, `screencapture`만 직접 요구하도록 줄였고, lock 파일은 UGUI 전이 의존성인 `ui/imgui`를 포함한 7개 항목으로 정리됐다. Windows 빌드, 릴리즈 패키징, Steamworks content 재생성, 배포 무결성 감사 127/127, 릴리즈 후보 감사 270/270을 실행했으며, 릴리즈 zip 크기는 약 59.0MB에서 58.3MB로 줄었다.
- 2026-06-08: 사용자가 심사 화면 상단 `신청서`/`소득 증명` 라벨이 여전히 어색하다고 지적했다. Codex는 각 서류 슬롯의 아이콘 아래에 별도 라벨판을 다시 만들고 `소득 증명`은 좁은 영역에서 `소득증명` 한 줄로 표시되도록 조정했다. QA 도구 Windows 빌드와 그래픽 모드 저해상도 UI 스모크를 실행해 `completed=true`, `evidenceLabelOverflowCount=0`, `invalidScreenshotCount=0`을 확인하고 1280x720 캡처를 직접 열어 라벨이 박스 안에 들어오는지 확인했다. 이후 플레이어용 Windows 릴리즈 빌드와 `CareReviewOffice_Windows_v0.3.0.zip`도 갱신했다.
- 2026-06-08: Codex는 구버전 리소스 백업으로 남아 있던 `Assets/Archive/DeprecatedResourceArt`가 플레이어 출시 프로젝트에는 필요 없는 자산이라고 판단했다. 해당 보관 폴더를 제거하고, 릴리즈 후보 감사와 배포 무결성 감사에 `Assets/Archive`가 다시 생기면 실패하는 검사를 추가했다.
- 2026-06-08: Codex는 `Resources/Art`에 남은 UI 생성 이미지 중 플레이어 릴리즈 화면에서 쓰지 않는 QA 로그 저장 전용 `ui_button_export_generated.png`를 확인했다. `TextButtonRole.Export` 전용 분기와 빌더 준비 목록, Resources allowlist에서 해당 자산을 제거했고, QA 설문/로그 버튼은 기존 분석/기본 버튼 스타일을 쓰도록 정리했다.
- 2026-06-08: Codex는 플레이어 패키지 최상위 파일을 점검해 `UnityCrashHandler64.exe`가 게임 플레이 자체에는 필요 없는 Unity 기본 크래시 핸들러임을 확인했다. 복사본에서 해당 파일을 제거한 뒤 15초 실행 테스트로 게임이 정상 기동되는지 확인했고, 빌드 후 `UnityCrashHandler64.exe`를 자동 삭제하도록 빌더와 패키지 allowlist를 갱신했다.
- 2026-06-08: Codex는 D3D12/DirectStorage 보조 파일도 줄일 수 있는지 검토했다. Windows 빌드를 Direct3D11 전용으로 바꾸는 실험 빌드는 생성됐지만, QA 저해상도 UI 스모크가 시작 로그 없이 반복 타임아웃되어 출시 안정성 근거가 부족하다고 판단했다. 해당 변경은 적용하지 않고 D3D12/DirectStorage 보조 파일은 유지하기로 했다.

## 계속 기록 규칙

앞으로 AI를 사용해 프로젝트 작업을 진행할 때마다 아래 항목을 짧게 남긴다.

- 날짜
- 사용자가 요청한 작업
- AI가 수행한 역할
- 실제로 변경된 파일 또는 산출물
- 실행한 테스트 또는 확인 방법
- 사람이 직접 확인해야 하는 남은 항목

기록은 대화 전문을 옮기지 않고, 제출자가 AI를 어떤 방식으로 활용했는지 설명할 수 있을 정도로 요약한다.

## AI 활용 방식

- 코드 분석: 기존 Unity 스크립트, 문서, 프로젝트 구조를 읽고 수정 범위를 파악했다.
- 구현 보조: 오디오 로딩 로직, 재생 연결, 테스트 로직을 코드에 반영했다.
- 자산 생성: 저작권 음원 대신 Python 기반 합성 스크립트로 WAV 파일을 직접 생성했다.
- 품질 확인: Unity 빌드와 런타임 스모크 테스트를 실행해 변경사항이 동작하는지 확인했다.
- 문서화: README, 개발 로그, 릴리즈 체크리스트, Steamworks 준비 문서에 변경 내용을 반영했다.
- 버전 관리: 백업용 zip 생성, git 커밋, GitHub 원격 저장소 푸시를 도왔다.

## 검증 내용

- 생성된 WAV 자산은 44.1kHz, 16-bit PCM 형식으로 확인했다.
- 오디오 스모크 테스트에서 BGM 5개와 SFX 20개가 모두 로드되는 것을 확인했다.
- 기본 실행, 저해상도 UI, 리포트 도달 스모크 테스트를 통해 주요 화면 흐름이 유지되는지 확인했다.
- GitHub 저장소에는 소스, 문서, 설정 파일을 올리고, 빌드 결과물과 Unity 캐시 등 로컬 산출물은 제외했다.

## 한계

- AI는 자동 테스트와 로그 확인을 수행했지만, 실제 스피커로 5분 이상 플레이하며 듣는 수동 청음 검수는 별도로 완료하지 않았다.
- 최종 판단과 배포 여부는 사용자가 확인해야 한다.

## 추가 작업 기록

- 2026-06-08: 사용자가 심사 화면 상단 서류 카드의 `신청서`/`소득증명` 라벨이 여전히 어색하다고 지적했다. Codex는 최근 저해상도 UI 캡처를 확인한 뒤, 서류 슬롯 전체를 중앙 신청서 폭 안으로 다시 맞추고 라벨판과 텍스트 영역을 넓혔다. QA 도구 Windows 빌드와 저해상도 UI 스모크 테스트를 실행해 `completed=true`, `evidenceLabelOverflowCount=0`, `invalidScreenshotCount=0`, `buttonTextOverflowCount=0`을 확인했고 1280x720/1920x1080 심사 화면 캡처를 직접 열어 배치를 확인했다.
- 2026-06-08: 사용자가 `신청서`/`소득증명` 라벨이 여전히 이상하다고 추가 피드백을 줬다. Codex는 서류 슬롯을 더 높은 카드형 영역으로 바꾸고 하단 캡션 판의 높이와 내부 여백을 키워 글자가 박스 경계에 걸쳐 보이지 않도록 조정했다. QA 도구 Windows 빌드와 저해상도 UI 스모크 테스트를 다시 실행해 `completed=true`, `screenshotCount=39`, `evidenceLabelOverflowCount=0`, `buttonTextOverflowCount=0`, `offscreenButtonCount=0`을 확인하고 1280x720 심사 화면 캡처를 직접 검토했다.
- 2026-06-08: 실제 출시 패키지에서 플레이어에게 필요하지 않은 `UnityCrashHandler64.exe`가 다시 포함되지 않도록 릴리즈 후보 감사와 배포 무결성 감사에 `플레이어 패키지 CrashHandler 제외` 항목을 추가했다. Windows 실행본, 릴리즈 폴더/zip, Steamworks content/zip을 모두 검사하도록 했고, 릴리즈 패키징과 Steamworks 패키징을 다시 실행한 뒤 배포 무결성 129/129, 릴리즈 후보 272/272 통과를 확인했다.
- 2026-06-08: 원본 Windows 실행본에도 불필요한 최상위 파일이 다시 생기지 않도록 `플레이어 패키지 최상위 구성 allowlist` 감사를 보강했다. 기존 릴리즈 폴더/zip과 Steamworks content/zip 검사에 `Builds/Windows` 직접 실행본 검사를 추가했고, 배포 무결성 129/129 및 릴리즈 후보 272/272 통과로 확인했다.
- 2026-06-08: QA/디버그 산출물 제외 감사가 폴더 검사에만 머물지 않도록 범위를 넓혔다. `Builds/Windows`, 릴리즈 폴더, Steamworks `content_windows`, 릴리즈 zip, Steamworks zip 내부 `content_windows` 엔트리까지 `pdb/mdb/log/tmp/qa/smoke/playtest/support_bundle/system_diagnostic` 흔적을 검사하도록 했고, 배포 무결성 130/130 및 릴리즈 후보 272/272 통과로 확인했다.
- 2026-06-08: 플레이어용 README와 릴리즈 manifest에도 내부/QA 문구가 zip 안으로 다시 들어오지 않도록 감사를 보강했다. 릴리즈 폴더/zip과 Steamworks `content_windows` 폴더/zip의 `README_KR.txt`, `RELEASE_MANIFEST.txt`를 같은 금지어 목록으로 검사하도록 했고, 배포 무결성 130/130 및 릴리즈 후보 272/272 통과로 확인했다.
- 2026-06-08: 플레이어 어셈블리 내부/QA 문자열 제외 감사도 zip 내부까지 확장했다. `Builds/Windows`, 릴리즈 폴더/zip, Steamworks `content_windows` 폴더/zip의 `Assembly-CSharp.dll`에서 자동화/플레이테스트/지원 번들/설문/CSV/HTML/QA 문자열이 남아 있지 않은지 검사하도록 했고, 배포 무결성 131/131 및 릴리즈 후보 272/272 통과로 확인했다.
- 2026-06-08: `Assets/Resources`에 남은 런타임 자산을 다시 대조했고, 예전에 제거한 `ui_button_export_generated.png`와 `.meta`가 다시 생기면 폐기 리소스 검사에서 실패하도록 보강했다. 실제 파일과 meta가 모두 없는지 확인했고, 배포 무결성 131/131 및 릴리즈 후보 272/272 통과를 확인했다.
- 2026-06-08: 릴리즈 폴더/zip과 Steamworks `content_windows` 폴더/zip이 현재 `Builds/Windows` 실행 페이로드와 같은 파일·해시를 갖는지 검사하는 미러링 감사를 추가했다. README와 릴리즈 manifest 2개만 패키지 문서로 허용하고 나머지 실행 파일은 상대 경로와 SHA256이 모두 같아야 통과하도록 했으며, 배포 무결성 132/132 및 릴리즈 후보 273/273 통과로 확인했다.
- 2026-06-08: 릴리즈/Steamworks 플레이어 패키지의 `README_KR.txt`와 `RELEASE_MANIFEST.txt`가 폴더와 zip 사이에서 서로 달라지지 않도록 `플레이어 패키지 README/manifest 문서 미러링` 감사를 추가했다. 릴리즈 폴더, 릴리즈 zip, Steamworks `content_windows` 폴더, Steamworks zip 내부 문서 SHA256을 모두 비교하도록 했고, 패키지와 Steamworks depot을 다시 생성한 뒤 배포 무결성 133/133 및 릴리즈 후보 274/274 통과를 확인했다.
- 2026-06-08: 출시 실행본이 개발 빌드나 디버거 대기 상태로 배포되지 않도록 `플레이어 boot.config 릴리즈 설정` 감사를 추가했다. `Builds/Windows`, 릴리즈 폴더/zip, Steamworks `content_windows` 폴더/zip의 `CareReviewOffice_Data/boot.config`가 `wait-for-native-debugger=0`을 포함하고 `development-player=1`, `wait-for-native-debugger=1`, `wait-for-managed-debugger=1`을 포함하지 않는지 검사하도록 했으며, 배포 무결성 134/134 및 릴리즈 후보 275/275 통과를 확인했다.
- 2026-06-08: 사용자가 `dstorage.dll` 누락 실행 오류를 보고했다. Codex는 `Builds/Windows`, 릴리즈 폴더, 릴리즈 zip, Steamworks `content_windows`, Steamworks zip에 `dstorage.dll`과 `dstoragecore.dll`이 모두 포함된 것을 확인했고, 플레이어 README 실행 방법에 두 DLL을 삭제하거나 이동하지 말라는 안내를 추가했다. 또한 `플레이어 DStorage 런타임 DLL 포함`, `릴리즈 README DStorage DLL 보존 안내`, `플레이어 패키지 프로젝트/소스 파일 제외` 감사를 추가해 배포 무결성 136/136 및 릴리즈 후보 278/278 통과를 확인했다.
- 2026-06-08: exe만 따로 실행할 때 생길 수 있는 필수 런타임 의존성 누락 문제를 더 줄이기 위해 README 실행 안내와 감사를 `D3D12/D3D12Core.dll`, `UnityPlayer.dll`, `dstorage.dll`, `dstoragecore.dll` 전체로 확장했다. `플레이어 필수 런타임 의존 파일 포함` 감사가 Windows 빌드, 릴리즈 폴더/zip, Steamworks `content_windows` 폴더/zip을 모두 확인하도록 했고, 릴리즈/Steamworks 패키지를 다시 생성한 뒤 배포 무결성 136/136 및 릴리즈 후보 278/278 통과를 확인했다.
- 2026-06-08: 필수 DLL 포함 감사가 실제 실행 오류를 막는지 확인하기 위해 릴리즈 폴더 `Builds/Release/CareReviewOffice_Windows_v0.3.0/CareReviewOffice.exe`를 같은 폴더에서 직접 기동했다. `Logs/runtime_release_launch_dependency_20260608_01.log`에서 Unity 엔진 초기화와 Direct3D renderer 생성이 확인됐고, `dstorage`, `missing`, `failed` 관련 의존성 오류가 없는 것을 확인했다.
- 2026-06-08: 사용자의 `dstorage.dll` 누락 오류 보고를 재발 방지 감사로 남기기 위해 `릴리즈 실행 의존성 스모크`를 추가했다. Unity 배치 메서드가 릴리즈 폴더에서 `CareReviewOffice.exe`를 직접 실행하고 `care_review_release_launch_dependency_smoke_result.json`에 엔진 초기화, renderer 초기화, DLL 누락 오류 부재, 8초 이상 실행 유지 여부를 기록하도록 했다. 실행 결과 `completed=true`, `dependencyErrorDetected=false`를 확인했고, 배포 무결성 137/137 및 릴리즈 후보 279/279 통과를 확인했다. Steam 공개 준비는 외부 액션 8개 때문에 계속 `steamPublicReleaseReady=false`로 남겼다.
- 2026-06-08: 출시 실행본에서 플레이어에게 필요하지 않은 Unity managed 모듈을 더 줄이기 위해 릴리즈 복사본에서 삭제 후보를 단계적으로 검증했다. Physics/Animation/AdaptivePerformance/Analytics 일부와 Accessibility/IMGUI/Input/Properties/SharedInternals/Subsystems/UIElements는 Unity 초기화에 필요해 삭제하지 않았고, 개별 실행 스모크를 통과한 `AndroidJNI`, `AssetBundle`, `Director`, `Grid`, `HierarchyCore`, `Multiplayer`, `ParticleSystem`, `Terrain`, `Tilemap`, `UnityConsent`, `UnityWebRequest`, `VFX`, `Video`, `VR`, `XR` managed 모듈 15개만 빌드 후처리에서 제거하도록 했다. Windows 빌드, 릴리즈 폴더/zip, Steamworks `content_windows` 폴더/zip에 해당 DLL이 다시 들어오면 실패하는 `플레이어 비게임 Unity 모듈 제외` 감사를 추가했고, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 138/138, 릴리즈 후보 280/280 통과를 확인했다.
- 2026-06-08: Codex는 릴리즈 폴더의 `MonoBleedingEdge/etc/mono` 내부에서 웹서비스 도움말과 브라우저 호환 DB 계열 파일이 플레이어 경험에 필요하지 않은지 복사본으로 검증했다. 복사본에서 `browscap.ini`, `DefaultWsdlHelpGenerator.aspx`, `web.config`, `settings.map`, `Compat.browser` 13개 파일을 제거한 뒤 `CareReviewOffice.exe`를 8초 이상 실행했고, Unity 엔진 초기화와 Direct3D renderer 생성, 의존성/타입 오류 부재를 확인했다. 해당 파일들을 빌드 후처리 제거 목록에 추가하고, 릴리즈 폴더/zip과 Steamworks `content_windows` 폴더/zip에 다시 들어오면 실패하는 `플레이어 Mono 웹 보조 파일 제외` 감사를 추가했다. Steamworks content 파일 수는 75개에서 62개로 줄었고, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 139/139, 릴리즈 후보 281/281 통과를 확인했다.
- 2026-06-08: Codex는 `Assets/Resources/Art`의 생성 UI 자산을 코드 참조 기준으로 다시 대조했다. `ui_focus_frame_generated.png`는 빌더 준비 목록과 Resources allowlist에만 남아 있고 런타임 `GeneratedUiSprite` 호출에서 쓰이지 않는 것을 확인해 파일과 `.meta`를 제거했다. 폐기 리소스 목록에도 추가해 해당 파일이 다시 들어오면 감사에서 실패하도록 했다. 릴리즈와 Steamworks content/zip을 재생성한 뒤 해당 파일이 산출물에 없음을 확인했고, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 139/139, 릴리즈 후보 281/281 통과를 확인했다.
- 2026-06-08: Codex는 남은 Mono/BCL 보조 파일 중 플레이어 경로에서 쓰지 않는 후보를 복사본으로 검증했다. `mconfig/config.xml`, `2.0/4.0/4.5 machine.config`, `MonoPosixHelper.dll`, `System.Configuration.dll`, `System.Numerics.dll`, `System.Xml.dll`을 개별 제거한 릴리즈 복사본이 8초 실행 스모크를 통과했고, 같은 파일을 모두 제거한 QA 도구 복사본도 `-careReviewSmokeTest`에서 `CARE_REVIEW_SMOKE_RESULT`를 출력하며 오류 없이 종료했다. 해당 파일들을 빌드 후처리 제거 목록에 추가하고, 릴리즈/Steamworks 폴더와 zip에 다시 들어오면 실패하는 managed library/Mono runtime 보조 파일 감사를 추가했다. Steamworks content 파일 수는 62개에서 54개로 줄었고, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 141/141, 릴리즈 후보 283/283 통과를 확인했다.
- 2026-06-08: Codex는 남은 managed DLL 중 추가로 줄일 수 있는 항목을 QA/릴리즈 복사본으로 다시 검증했다. `Mono.Security.dll`과 `Unity.Scripting.dll`은 QA 기본 스모크와 릴리즈 8초 실행 의존성 스모크를 통과해 빌드 후처리 제거 목록에 추가했다. 반대로 `System.Core.dll`은 UI 초기화 중 `TypeLoadException`/`MissingMethodException`, `UnityEngine.TextRenderingModule.dll`과 Unity Analytics 모듈은 플레이어 크래시, `UnityEngine.TextCoreTextEngineModule.dll`은 `FileNotFoundException`, `UnityEngine.TextCoreFontEngineModule.dll`은 실제 저해상도 렌더링 경로에서 TextCore assembly load 경고가 발생해 유지했다. 최종 Steamworks content 파일 수는 54개에서 52개로 줄었고, 기본 QA 스모크, 저해상도 UI QA `completed=true`/39장/invalid 0, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 141/141, 릴리즈 후보 283/283 통과를 확인했다.
- 2026-06-08: Codex는 플레이어 아트 텍스처가 `isReadable=true`와 `Uncompressed` import 설정으로 강제되어 있어, 픽셀을 읽지 않는 런타임 이미지까지 과한 형태로 빌드에 들어가는 문제를 확인했다. 코드에서 리소스 텍스처는 `Sprite.Create`와 width/height 참조만 사용하고 `GetPixels`를 하지 않는 것을 확인한 뒤, 빌더의 `PrepareTexture`를 readable false와 compressed import로 바꿨다. `플레이어 아트 텍스처 readable/무압축 제외` 감사를 추가해 재발을 막았고, QA 도구 빌드와 저해상도 UI QA `completed=true`/39장/invalid 0으로 화면 경로를 확인했다. 릴리즈 폴더 총 용량은 약 121.6MB에서 117.7MB로, 릴리즈 zip은 약 56.5MB에서 53.1MB로 줄었으며, 배포 무결성 142/142와 릴리즈 후보 284/284 통과를 확인했다.
- 2026-06-08: Codex는 직접 합성한 WAV 오디오 자산 25개가 모두 같은 import 설정으로 남아 있는지 확인했다. BGM 5개는 stereo 44.1k 루프이므로 stereo를 유지하되 streaming Vorbis와 background load로 바꾸고, 짧은 SFX 20개는 mono ADPCM/decompress-on-load로 명시했다. `플레이어 오디오 import 출시 최적화` 감사를 추가해 BGM/SFX 설정이 되돌아가면 실패하도록 했고, 오디오 스모크와 기본 QA 스모크를 통과시켰다. 릴리즈 폴더 총 용량은 약 117.7MB에서 115.4MB로, 릴리즈 zip은 약 53.1MB에서 50.8MB로 줄었으며, 릴리즈 실행 의존성 스모크 `completed=true`, 배포 무결성 143/143, 릴리즈 후보 285/285 통과를 확인했다.
- 2026-06-08: 사용자는 `Papers, Please` 벤치마크 대비 현재 화면이 상용 게임처럼 보이지 않고 조잡하다고 판단했다. Codex는 `Docs/상용_비주얼_개선_작업표.md`와 `Docs/상용_비주얼_작업_방향.md`를 작성해 정보 밀도, UI 장식 과다, 서류 처리 감각 부족, 텍스트 위계 문제를 정리했고, 1차 구현으로 심사 화면의 서류 슬롯을 문서 탭/캡션 구조로 바꾸고 결정 버튼 5개를 도장 패드/트레이 구조로 재구성했다. QA 도구 Windows 빌드와 저해상도 UI 스모크는 통과했지만, 현재 환경의 PNG 캡처가 검게 저장되어 실제 사람 눈 시각 검증은 정상 캡처 또는 수동 실행 화면에서 다시 해야 한다고 기록했다.
- 2026-06-08: Codex는 상용 비주얼 작업 방향의 `보조 패널 축약` 후속으로 심사 화면의 운영 기준, 지표, 판단 근거, 판단 직후 피드백 문구를 줄였다. 기본 안내를 신청서/소득증명 대조와 도장 행동 중심으로 바꾸고, 운영 기준 패널은 `오늘 운영 초점`과 짧은 판단 순서로 압축했으며, 판단 직후 피드백은 확정/권장/근거/다음 사례가 먼저 보이게 정리했다. QA 도구 Windows 빌드와 저해상도 UI 스모크는 통과했고, 판단 비교/운영 초점 스모크는 JSON 기준 통과했지만 숨김 실행 프로세스가 자동 종료하지 않아 수동으로 종료했다.
- 2026-06-08: Codex는 상용 비주얼 작업 방향의 `메인 메뉴` 항목을 이어 받아 메인 메뉴를 주 행동 중심으로 재배치했다. 오른쪽 버튼 영역을 시작/이어하기 중심의 주 행동 레일, 기록/성과/설정 보조 레일, 목표 버튼 선반으로 나누고, 왼쪽 설명과 하단 상태문에서 빌드/설정/긴 단축키 나열을 제거했다. QA 도구 Windows 빌드, 메인 메뉴 운영 기준 스모크, 포커스 내비게이션 스모크는 통과했고, 저해상도 UI 스모크는 텍스트/오버플로 지표는 통과했지만 현재 환경에서 캡처가 모두 검게 저장되어 `invalidScreenshotCount=39`로 남았다.
- 2026-06-08: Codex는 상용 비주얼 작업 방향의 `정책/기준표` 항목을 이어 받아 정책 오버레이를 결론 우선 구조로 정리했다. 상단 첫 줄을 `결론: {권장 판단} 기준`으로 바꾸고, 사례 근거와 도장별 예상 변화 문구를 줄였으며, 오른쪽 기준 비교 영역은 위험 최소/예산 안정/권장 일치 결론과 5개 운영 기준 막대가 먼저 보이도록 재배치했다. QA 도구 Windows 빌드, 정책 기준표 스모크, 정책 시뮬레이터 스모크는 통과했고, 저해상도 UI 텍스트/오버플로 지표도 통과했지만 현재 환경에서 스크린샷 생성/유효성은 계속 신뢰하기 어렵다고 기록했다.
- 2026-06-08: Codex는 상용 비주얼 작업 방향의 `최종 리포트` 항목을 이어 받아 리포트 화면을 결과 카드 중심으로 정리했다. 엔딩 이미지를 키우고 운영 등급/핵심 결과 카드를 상단에 키웠으며, 주요 지표 게이지를 로그보다 먼저 보이게 내리고, 사례 로그/후속 로그/긴 해석 문장은 보조 상세로 낮췄다. QA 도구 Windows 빌드, 운영 등급 스모크, 캠페인 챌린지 스모크, UI 정리 스모크는 통과했고, 저해상도 UI 텍스트/오버플로 지표도 통과했지만 캡처 유효성은 현재 환경 문제로 계속 신뢰하기 어렵다고 기록했다.
- 2026-06-08: Codex는 검은 PNG로 저장되던 자동 캡처 문제를 줄이기 위해 `ScreenCapture` 실패 시 캔버스를 임시 `ScreenSpaceCamera`로 전환하고 `RenderTexture`로 PNG를 쓰는 폴백을 추가했다. 저해상도 UI 스모크는 `completed=true`, `invalidScreenshotCount=0`으로 통과했고, 정책 시뮬레이터도 `screenshotCaptured=true`로 통과했다. 정상 PNG를 직접 열어 메인 메뉴, 심사 화면, 정책 화면, 최종 리포트를 사람 눈으로 1차 평가했으며, 심사 화면은 오른쪽 보조 패널과 도장 트레이 밀도 때문에 4/5 기준에 아직 못 미친다고 기록했다.
- 2026-06-08: Codex는 1차 사람 눈 QA에서 부족하다고 판단한 심사 화면을 이어 수정했다. 오른쪽 운영 기준/지표/판단 근거 패널의 크기와 알파를 줄이고, 하단 도장 트레이와 도장 버튼 장식을 낮췄으며, 도장 내부 중복 결정명 라벨을 숨겨 긴 `추가조사` 라벨이 어긋나 보이는 문제를 줄였다. QA 도구 Windows 빌드, 저해상도 UI 스모크, 운영 초점 스모크, 일일 진행 상태 스모크, 판단 근거 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 심사 화면 캡처를 직접 열어 확인한 결과 이전보다 개선됐지만, 오른쪽 운영 기준과 도장별 예상 변화는 다음 단계에서 더 줄일 여지가 있다고 기록했다.
- 2026-06-08: Codex는 상용 비주얼 작업 방향의 다음 항목으로 메인 메뉴 추천 브리핑과 목표 선반의 밀도를 낮췄다. `추천 회차 브리핑`을 `다음 근무 초점`으로 바꾸고 `직전 약점`을 `보정 초점`으로 줄였으며, 브리핑/상태 박스/목표 선반의 높이와 배경 알파, 글자 크기를 낮춰 시작/이어하기 버튼이 먼저 읽히게 했다. 메인 메뉴 운영 기준 스모크, 목표 사례/성장 루프 스모크, 포커스 내비게이션 스모크, 저해상도 UI 스모크를 실행해 모두 `completed=true`로 통과했고, 정상 1280x720/1920x1080 메인 메뉴 캡처를 직접 확인했다. 하단 목표 선반은 여전히 관리툴 느낌이 남아 다음 단계 후보로 기록했다.
- 2026-06-08: Codex는 메인 메뉴 하단 목표 선반을 대표 목표 중심으로 한 번 더 정리했다. `목표 사례` 버튼을 대표 목표로 남기고, `성장 심사`, `후속 심사`, `추천 전환`은 작은 보조 탭으로 낮췄으며 성장/후속 목표의 상세 힌트는 기능 검증용 문자열로 유지하되 첫 화면에서는 보이지 않게 했다. QA 도구 Windows 빌드, 메인 메뉴 운영 기준 스모크, 목표 사례/성장 루프 스모크, 포커스 내비게이션 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 메인 메뉴 캡처를 직접 확인해 시작/이어하기 우선순위와 대표 목표 중심 배치가 개선됐다고 기록했다.
- 2026-06-08: Codex는 심사 화면 오른쪽 운영 기준 패널의 문장 수와 패널 높이를 줄였다. 운영 기준, 오늘 운영 초점, 오늘 진행, 판단 순서를 3-4줄 안의 업무 메모 형태로 압축하고, 중복되는 `남은 서류` 항목과 긴 판단 순서 문장을 제거했다. 운영 기준 책 배경과 텍스트 박스 높이도 줄여 빈 장식 면적을 낮췄다. QA 도구 Windows 빌드, 운영 초점 스모크, 일일 진행 상태 스모크, 메인 메뉴 목표 사례/성장 루프 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 심사 화면 캡처를 직접 확인해 오른쪽 패널이 중앙 신청서와 덜 경쟁한다고 기록했다.
- 2026-06-08: Codex는 정책/기준표 화면의 `도장별 예상 변화` 표 밀도를 줄였다. 각 도장별 3줄 반복 설명을 1줄 비교표로 압축하고, `예산/안정/형평/위험`은 헤더에 남긴 뒤 행에서는 `예/안/형/위` 약어로 표시했다. 예상 변화 표 박스 높이와 알파도 낮춰 빈 장식 면적과 관리툴 느낌을 줄였다. QA 도구 Windows 빌드, 정책 기준표 스모크, 정책 시뮬레이터 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. `care_review_policy_simulator.png` 캡처를 직접 확인해 가운데 표가 이전보다 덜 빽빽하다고 기록했다.
- 2026-06-08: Codex는 심사 화면 하단 도장별 예상 변화 프리뷰가 항상 5개 같은 강도로 보여 도장 트레이보다 먼저 읽히는 문제를 정리했다. 테스트가 요구하는 다섯 판단, `예산`, `위험`, `권장`, `대체` 문자열은 유지하되 프리뷰 레일 폭과 배경 알파를 낮추고, 권장/대체/일반 판단의 폰트와 텍스트 알파를 다르게 적용해 권장 판단만 또렷하게 보이도록 했다. QA 도구 Windows 빌드, 결정 프리뷰 스모크, UI 정리 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 심사 화면 캡처를 열어 하단 프리뷰가 이전보다 덜 경쟁한다고 기록했다.
- 2026-06-08: Codex는 메인 메뉴 좌우 패널이 서로 다른 질감으로 보여 조잡하게 느껴지는 문제를 이어 정리했다. 왼쪽 문서 패널의 폴더/종이 틴트 크기와 알파를 낮추고, 오른쪽 주 행동/보조 행동/목표 레일이 같은 크롬 규칙을 쓰도록 `Command Rail`과 `Objective Shelf`를 레일 크롬 대상으로 묶었다. 메뉴 버튼의 역할 색상은 유지하되 알파를 낮춰 테두리 대비를 줄였고, 브리핑 박스는 스모크의 대비 조건 때문에 기존 대비 알파를 유지했다. QA 도구 Windows 빌드, 메인 메뉴 운영 기준 스모크, 목표 사례/성장 루프 스모크, 포커스 내비게이션 스모크, UI 정리 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 메인 메뉴 캡처를 열어 좌우 패널이 이전보다 덜 따로 보인다고 기록했다.
- 2026-06-08: Codex는 정책/기준표 화면의 가운데 빈 여백과 오른쪽 하단 쏠림을 줄였다. 왼쪽 사례 근거 패널 높이를 낮추고, 가운데 도장별 예상 변화 표를 결론 표처럼 위로 정리했으며, `위험 최소`, `예산 안정`, `권장 일치` 추천 카드 3개를 오른쪽 하단에서 가운데 결정표 아래로 옮겼다. 오른쪽 기준 비교 행 간격과 행 카드 높이도 줄여 상단 결론과 5개 비교 행이 한 묶음으로 읽히게 했다. QA 도구 Windows 빌드, 정책 기준표 스모크, 정책 시뮬레이터 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. `care_review_policy_simulator.png` 캡처를 직접 확인해 가운데 빈 영역과 오른쪽 쏠림이 줄었다고 기록했다.
- 2026-06-08: Codex는 심사 화면의 중앙 신청서, 오른쪽 보조 패널, 하단 도장 트레이가 서로 다른 질감처럼 보이는 문제를 이어 정리했다. 중앙 서류에는 낮은 알파 그림자와 낮춘 종이 틴트를 적용하고, 오른쪽 운영 기준/지표/판단 근거 뒤에는 낮은 알파의 보조 레일을 추가했으며, 하단 도장 트레이도 같은 레일 크롬 규칙에 묶었다. 오른쪽 보조 패널과 하단 트레이 알파를 낮춰 중앙 신청서와 덜 경쟁하게 했고, 심사 화면용 패널 크롬은 낮은 틴트 규칙을 따르게 했다. QA 도구 Windows 빌드, 운영 초점 스모크, 일일 진행 상태 스모크, 판단 근거 스모크, UI 정리 스모크, 저해상도 UI 스모크를 실행했고 모두 `completed=true`로 통과했다. 정상 1280x720/1920x1080 심사 화면 캡처를 열어 세 영역의 테두리 대비가 이전보다 가까워졌다고 기록했다.
- 2026-06-08: Codex는 전 화면 공통 버튼의 생성 스프라이트 테두리와 코너 장식이 과하게 도드라지는 문제를 이어 정리했다. 생성 버튼의 기본 틴트 알파, 왼쪽 액센트 폭/알파, 그림자 강도와 거리를 줄였고, 폴백 버튼의 상하 엣지와 그림자도 같은 방향으로 낮췄다. `Button Accent`와 `Button Generated UI` 구조는 유지해 기존 역할 위계 검증을 보존했으며, QA 도구 Windows 빌드, UI 정리 스모크, 포커스 내비게이션 스모크, 그래픽 경로 저해상도 UI 스모크를 실행해 모두 `completed=true`를 확인했다. 정상 1280x720/1920x1080 메인 메뉴와 심사 화면 캡처를 직접 확인해 버튼 장식이 이전보다 덜 경쟁한다고 기록했다.
- 2026-06-08: Codex는 정책/기준표 모달 하단의 빈 종이 영역이 넓어 보이는 문제를 이어 정리했다. 모달 종이 높이를 줄이고, `위험 최소`, `예산 안정`, `권장 일치` 추천 카드를 아래로 내려 결론 footer처럼 배치했으며, 힌트 문장과 닫기 버튼을 더 짧고 가깝게 조정했다. QA 도구 Windows 빌드, 정책 기준표 스모크, 정책 시뮬레이터 스모크, 그래픽 경로 저해상도 UI 스모크를 실행해 모두 `completed=true`를 확인했고, `care_review_policy_simulator.png` 캡처를 열어 하단 여백이 이전보다 덜 비어 보인다고 기록했다.
- 2026-06-08: Codex는 최신 캡처 기준으로 메인 메뉴, 심사 화면, 정책 모달, 최종 리포트 화면을 다시 사람 눈 QA로 점수화했다. 메인/심사/정책은 4/5로 보았고, 리포트는 처음에 `가족 유형 판단 지도` 설명 문장과 막대가 겹쳐 3/5로 판단했다. 이어서 가족 지도 행 생성 좌표와 갱신 좌표를 맞추고 설명을 2줄로 줄여 리포트도 4/5 수준으로 끌어올렸다. QA 도구 Windows 빌드, UI 정리 스모크, 그래픽 경로 저해상도 UI 스모크를 실행해 `completed=true`, `invalidScreenshotCount=0`, 버튼/증빙 오버플로 0을 확인했고, 1280x720/1920x1080 리포트 캡처를 직접 확인했다.
- 2026-06-08: Codex는 버튼/모달 원본 스프라이트가 아직 금속 코너와 강한 게임 UI 장식처럼 보인다는 평가를 바탕으로 `ui_button_*_generated.png`, `ui_panel_*_generated.png`, `ui_action_rail_generated.png`, `ui_hotkey_badge_generated.png` 10개를 로컬 절차 생성으로 다시 만들었다. 외부 이미지 없이 낮은 그림자, 무광 색면, 얇은 내부선, 종이/파일철 질감으로 바꿨고, 생성 중 투명 영역에 질감 선이 새는 문제는 본체 내부 클리핑으로 다시 생성해 해결했다. 같은 결과를 재현할 수 있도록 `Tools/generate_office_ui_kit.ps1`를 추가하고 실행을 확인했다. QA 도구 Windows 빌드, UI 정리 스모크, 포커스 내비게이션 스모크, 그래픽 경로 저해상도 UI 스모크, 정책 시뮬레이터 스모크를 실행해 모두 `completed=true`를 확인했고, 메인/심사/정책/리포트 캡처를 직접 열어 장식성이 줄고 가독성이 유지되는지 확인했다.
- 2026-06-08: Codex는 1280x720 리포트 화면에서 `재검토 큐`와 하단 `다음 목표` 카드 문장이 과밀해 보이는 문제를 이어 정리했다. `재검토 큐`는 압박 지표와 대표 사례 2건 중심으로 줄이고, 하단 목표 카드는 필수 테스트 키워드를 유지한 압축 생성 함수로 바꿨다. QA 도구 Windows 빌드, UI 정리 스모크, 그래픽 경로 저해상도 UI 스모크, 캠페인 챌린지 스모크를 통과했고, 1280x720/1920x1080 리포트 캡처를 직접 열어 문장이 박스 밖으로 내려가지 않는지 확인했다. 추가로 돌린 일부 장기 루프 스모크는 브리핑/메뉴 상태/리포트 요약 조건에서 실패해 별도 회귀 점검 후보로 남겼다.
- 2026-06-08: Codex는 리포트 하단 카드 압축 뒤 실패 후보로 남겼던 장기 루프 스모크를 다시 확인했다. 메인 메뉴 성과 줄에는 `상위 배지`와 기존 `성과 배지/다음 성과/A/성과 보기` 토큰을 모두 유지하되 폭을 76 이하로 맞췄고, 성과 기반 추천 대기는 `장기 추천` 문구를 복구했다. 또한 이의제기 리포트 요약을 `이의제기 검토 · 보정` 구조로 맞추고, 판단 비교 연습 추천 캠페인의 첫 브리핑에 비교 연습 목표와 기준 사례를 추가했다. 판단 비교 연습, 이의제기 검토, 개인화 추천, 상위 반복 챌린지, 저해상도 UI 스모크가 모두 `completed=true`로 통과했으며, 1280x720 메인 메뉴 캡처를 직접 확인했다.
- 2026-06-08: Codex는 최종 리포트 하단 `다음 목표` 카드의 긴 문장이 말줄임으로 잘리는 문제를 이어 정리했다. 목표 종류별로 표시 문장을 따로 압축해 이의제기 보정, 성장 후속, 성과 목표, 판단 복기, 비교 연습 목표가 짧은 업무 메모처럼 보이게 했고, 테스트가 요구하는 핵심 토큰은 근거 줄에 보존했다. 보정 목표 브리핑에는 `보정 조치/근거`와 `고위험 지연 기준 확인`을 되살렸다. QA 도구 Windows 빌드와 캠페인 챌린지, 판단 비교 연습, 사례 자료실/보정 루프, 개인화 추천, 상위 반복 챌린지/단계, 추천 복기, 저해상도 UI 스모크를 통과했고, 1280x720/1920x1080 리포트 캡처를 직접 확인했다.
- 2026-06-09: Codex는 최종 리포트 왼쪽 엔딩 설명 카드가 줄어든 문장에 비해 어두운 빈 면적이 큰 문제를 이어 정리했다. 엔딩 카드를 `최종 엔딩`, 요약, `후속:`, `다음:` 4줄 중심으로 줄이고, 조사 후속 기록 토큰은 유지했으며, 카드 높이를 낮춰 1280x720에서 덜 무겁게 보이도록 했다. QA 도구 Windows 빌드, 조사 후속 에필로그, 캠페인 등급, UI 정리, 저해상도 UI 스모크를 통과했고, 1280x720/1920x1080 리포트 캡처를 직접 확인했다.
- 2026-06-09: Codex는 메인 메뉴 하단 목표 선반의 보조 탭이 1280x720에서 책상 배경과 너무 섞여 보이는 문제를 이어 정리했다. `새 캠페인 시작`과 `이어하기` 우선순위는 유지하면서 목표 선반 배경, 성장/후속 보조 목표 버튼, `추천 1/2` 탭의 알파와 글자 크기만 소폭 올렸다. QA 도구 Windows 빌드, 메인 메뉴 목표 사례/성장 루프 스모크, 저해상도 UI 스모크를 통과했고, 1280x720/1920x1080 메인 메뉴 캡처를 직접 확인했다.
- 2026-06-09: Codex는 1280x720 성과 기록 화면에서 성과 카드 내부 설명문이 너무 작고 연하게 보이는 문제를 확인했다. 성과 카드 행과 내부 텍스트 박스를 키우고, 카드 문구를 긴 설명 대신 `[해금/잠김] 성과명`과 진행 상태 1줄 중심으로 압축했으며, 반복/보정/비교/성장 목표 스모크가 요구하는 핵심 토큰은 유지했다. QA 도구 Windows 빌드, 성과 스모크, 저해상도 UI 스모크를 통과했고, 1280x720/1920x1080 성과 기록 캡처를 직접 확인했다.
- 2026-06-09: Codex는 캠페인 기록 화면이 내부 로그처럼 과밀해 보이는 문제를 이어 정리했다. 본문 요약과 최근 완료 회차 표, 선택 회차 상세 문장을 줄이면서 스모크가 확인하는 동일 사례 회고, 목표 재도전 결과, 조사 타임라인, 보정 액션 플랜, 기록 복귀 안내 토큰은 유지했다. 사용자가 보고한 `CareReviewOffice_Data` 폴더 누락 오류는 실행 중이던 QA 플레이어가 빌드 폴더 삭제를 막아 생긴 상태로 판단했고, 해당 프로세스를 종료한 뒤 QA 빌드를 다시 만들어 exe와 `_Data` 폴더 구조를 복구했다. QA 도구 Windows 빌드, 캠페인 기록 스모크, 저해상도 UI 스모크를 통과했고 1280x720/1920x1080 캠페인 기록 캡처를 직접 확인했다.
- 2026-06-09: Codex는 `CareReviewOffice_Data` 폴더 누락 오류가 다시 생기지 않도록 빌드 스크립트를 보강했다. `ResetBuildDirectory`가 빌드 폴더를 삭제하기 전에 같은 폴더의 `CareReviewOffice.exe`로 실행 중인 프로세스만 찾아 종료하도록 했고, QA 플레이어를 일부러 실행한 상태에서 `BuildWindowsQaTools`를 다시 돌려 자동 종료 로그와 빌드 성공을 확인했다. 이어 기본 QA 스모크를 실행해 `CARE_REVIEW_SMOKE_RESULT ... cases=40 logs=40 highestDay=5`를 확인했다.
- 2026-06-09: Codex는 캠페인 기록 화면의 상단 본문이 여전히 내부 로그처럼 길게 보이는 문제를 이어 정리했다. 본문 박스를 `요약 / 최근 회차 / 선택 상세` 안내 중심으로 줄이고 최근 회차를 3행으로 제한했으며, 선택 회차의 결과/판단/대표 사례/조사/판단 복기는 하단 상세 패널로 분리했다. 필터 스모크가 요구하는 성장, 후속, 이의제기 보정, 보정 추천 증거 문구는 필터가 켜졌을 때만 본문에 남겼다. QA 도구 Windows 빌드, 캠페인 기록 스모크, 저해상도 UI 스모크를 통과했고, 1280x720/1920x1080 캠페인 기록 캡처를 직접 확인했다.
- 2026-06-09: Codex는 최종 리포트 오른쪽 `재검토 큐` 카드에서 대표 사례 사유가 `서류 보...`처럼 잘려 보이는 문제를 확인했다. AI는 기존 긴 분석 문장을 유지하면서 리포트 표시용 라벨만 `권장 불일치`, `고위험 지연`, `고비용 지원`, `서류 보강` 등 최대 2개로 압축하는 코드를 제안하고 적용했다. QA 도구 Windows 빌드, 캠페인 등급/리포트 도달 스모크, UI 정리 스모크, 저해상도 UI 스모크를 통과했고 1280x720/1920x1080 리포트 캡처를 직접 열어 줄임표 문제가 사라졌는지 확인했다.
- 2026-06-09: Codex는 상용 비주얼 작업 방향에 맞춰 메인 메뉴, 심사 화면, 정책/기준표, 최종 리포트의 핵심 화면 재점검을 진행했다. AI는 최신 1280x720 메인/심사/리포트 캡처와 1920x1080 리포트 캡처를 직접 확인하고, 정책 기준표는 최신 QA 빌드 스모크 통과와 직전 정책 캡처를 근거로 평가했다. 정책 시뮬레이터 캡처 스모크는 플레이어가 자동 종료하지 않아 최신 PNG 증거를 확보하지 못했고, 이 한계를 다음 작업 후보로 기록했다.
- 2026-06-09: Codex는 정책 시뮬레이터 캡처 스모크가 비배치 그래픽 실행에서 시작되지 않는 원인을 분석했다. AI는 `AutomationAllowedForCurrentProcess`와 비주얼 자동화 허용 목록을 대조해 `-careReviewPolicySimulatorSmokeTest`가 누락된 것을 확인했고, 해당 플래그를 추가했다. 이후 QA 도구 Windows 빌드와 그래픽 정책 시뮬레이터 스모크를 실행해 `completed=True`, `screenshotCaptured=true`를 확인하고 최신 `care_review_policy_simulator.png`를 직접 열어 정책 화면을 재점검했다.
- 2026-06-09: Codex는 최신 정책 기준표 캡처에서 하단 종이 여백이 넓어 보이는 문제를 확인했다. AI는 정책 모달의 종이 패널 높이, 하단 힌트, `닫기` 버튼 좌표를 조정해 하단 조작부가 더 단단히 붙어 보이도록 수정했다. QA 도구 Windows 빌드, 정책 시뮬레이터 그래픽 스모크, 저해상도 UI 스모크를 통과했고, 최신 `care_review_policy_simulator.png`를 열어 하단 여백이 줄었는지 확인했다.
- 2026-06-09: Codex는 심사 화면 오른쪽 운영 기준/지표/판단 근거 패널이 중앙 서류보다 강하게 보이는 문제를 이어 점검했다. AI는 오른쪽 보조 레일의 알파와 패널 높이, 운영 초점 문구, 지표 표시 줄 수를 조정했고, 첫 수정에서 `누락/민원/조사`가 보이지 않는 것을 캡처로 확인해 2줄 지표 문장으로 다시 보정했다. QA 도구 Windows 빌드, 운영 초점 스모크, 일일 진행 상태 스모크, 저해상도 UI 스모크를 통과했고 1280x720/1920x1080 심사 화면 캡처를 직접 확인했다.
